# Figure 4 `draft_v1` — immutable approved bundle

This directory freezes the user-approved four-panel Figure 4 on
2026-09-03. Do not edit or overwrite files
in this directory. Any later visual or analytical revision must use a new sibling
version directory. The evidence status remains `REPRODUCED (descriptive)`;
`draft_v1` is now a visual version lock, not the evidence label `FROZEN`.

## Release files

- `figure4_transition_dynamics_v1.pdf`: publication-facing vector figure.
- `figure4_transition_dynamics_v1.png`: 300-dpi review rendering.
- `figure4_panel_a_data.csv` through `figure4_panel_d_data.csv`: exact plotted data.
- `figure4_checks.json`: scientific, denominator, masking, and layout invariants.
- `figure4_manifest.json`: source identities, hashes, filters, exclusions, and outputs.
- `figure4_caption.md` and `figure4_note.md`: caption-ready interpretation and caveats.
- `code/`: exact plotting code, style, dependency versions, and font at approval.
- `documents/`: scientific-plan, claim-ledger, city-order, and information-gate
  snapshots at approval.
- `freeze_manifest.json`: SHA-256 inventory of the immutable checkpoint.

The approved page is 8.85 by 6.30 inches. Panels a and b share the city axis;
panels c and d occupy the aligned lower row. The single legend lies between the
rows, panel a uses a compact horizontal colorbar, and all city labels are black.

## Scientific result

The primary transition estimand is the new/existing ratio of anchor PV area per
corresponding plan-view roof-mask area at risk. The prespecified information gate
retains 55 of 67
full-AOI native adjacent transitions and hatches the remaining
12 rather than treating them as zero. The primary
roof-area-normalized direction switches in 4
cities (Miami, Charlotte, New York City, Boston). The lighter Building-count-normalized PV-area-yield
comparator switches in 7
cities (Miami, Philadelphia, Charlotte, Chicago, Phoenix, New York City, Boston).

These are observed-transition descriptors, not annual volatility, a permanent
city type, usable-roof utilization, measured nameplate capacity, a policy response,
or a causal effect. Cohort intervals may have unequal duration, and no
continuous-outcome inferential intervals are displayed.
